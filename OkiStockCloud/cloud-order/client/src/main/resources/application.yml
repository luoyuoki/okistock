server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    instance-id: order-client-8083
    prefer-ip-address: true

spring:
  application:
    name: order-client